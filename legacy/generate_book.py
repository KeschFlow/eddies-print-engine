# legacy/generate_book.py
# Legacy-CLI (optional). Die produktive Version ist app.py (Streamlit).
# Zweck: Historie bewahren, Root clean halten.

def main():
    print("Legacy CLI. Bitte nutze die Streamlit-App: streamlit run app.py")

if __name__ == "__main__":
    main()

    print(f"--- FERTIG: {pages} Seiten generiert in {path} ---")

if __name__ == "__main__":
    generate_book(30)
